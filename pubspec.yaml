name: examforge
description: ExamForge V2
publish_to: "none"
version: 0.2.0+2
environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  shared_preferences: ^2.3.2
  sqflite: ^2.4.1
  path: ^1.9.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/data/
    - assets/data/syllabus/
    - assets/data/syllabus/neet/
    - assets/data/questions/
    - assets/data/questions/neet/
    - assets/data/schema/
